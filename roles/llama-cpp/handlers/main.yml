---
- name: Restart llama-swap
  become: true
  ansible.builtin.systemd_service:
    name: llama-swap.service
    state: restarted

- name: Refresh shared library cache
  become: true
  ansible.builtin.command:
    cmd: /usr/sbin/ldconfig
  changed_when: true
