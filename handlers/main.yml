---
# - name: Reload user systemd
#   ansible.builtin.systemd_service:
#     scope: user
#     daemon_reload: true

- name: Reload systemd
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true
